# 판매자용 메모 (크몽 등)

짧은 판매·납품 체크리스트입니다. 구매자 안내서는 `설명서_실행방법.md`, `설명서_인스타연결.md`를 전달하세요.

## 납품 단위

- **고객(매장)마다** `clients/<고객명>/` 폴더를 두고 `profile.json` + `outputs/` 로 관리합니다.
- 샘플은 `clients/demo/` 입니다. 납품 zip에는 데모만 두고, 실고객 토큰·산출물은 넣지 마세요.

## 콘셉트 팩

- 카페 전용이 아닙니다. UI에서 **업종/콘셉트**를 고릅니다. (`cafe`, `shopping`, `academy`, `clinic`, `real_estate`, `restaurant`, `personal_brand`, `custom`)
- 훅·CTA·사진 키워드가 콘셉트에 맞춰집니다. 정의는 `src/concepts.py`.

## 절대 하지 말 것

- **비공식 인스타 봇 / 셀레니움 로그인 자동화** 제공·약속 금지. 공식 Graph API만.
- 구매자 `.env` 토큰을 판매자 PC·채팅·깃에 남기지 않기.
- zip에 `.env`, `.venv`, `__pycache__`, 실고객 `outputs` 대량 PNG, 토큰 파일 포함 금지.

## 토큰·발행 책임

- Meta 앱 생성, 테스터, 토큰, `IG_USER_ID`는 **구매자 책임**으로 안내합니다.
- 판매자는 “연결 방법 문서 + dry-run 확인”까지를 기본 지원 범위로 두는 것을 권장합니다.
- 이미지 공개 URL은 앱에 **임시 업로드 경로**가 이미 있습니다. CDN은 선택입니다.

## 면책 (구매자 안내 문구 예시)

> 본 소프트웨어는 공식 Meta Instagram Graph / Instagram Login API만 사용합니다.  
> 액세스 토큰·계정 권한·Meta 정책 준수·게시물 내용에 대한 책임은 사용자에게 있습니다.  
> 비공식 자동화로 인한 계정 제재에 대해 판매자는 책임지지 않습니다.  
> 토큰이 유출되지 않도록 `.env`를 공유·업로드하지 마세요.

## 판매용 zip 체크

- [ ] 코드 + fonts + requirements + run.bat/run.sh + `.env.example`
- [ ] 세 설명서 포함
- [ ] `.env` / `.venv` / `.git` / 캐시 이미지 / 비밀값 제외
- [ ] README에 설명서 링크
