# 판매자용 메모 (크몽 등)

구매자는 **개발 지식이 없습니다.** 납품의 기본은 **웹 링크 하나**입니다.  
zip / Python / `.env` 는 고급·선택입니다.

구매자 안내: `설명서_실행방법.md` (웹 우선)  
토큰 발급(판매자·기술): `설명서_인스타연결.md`

## 권장 납품 방식

1. GitHub `newbi-1/instagram-cardnews` 를 Streamlit Community Cloud에 배포
2. Main file = `app_simple.py`
3. 구매자에게 **공개 URL만** 전달
4. 인스타 올리기가 필요하면, 판매자가 발급한 **연결 키 1·2**만 안내 (화면 문구와 동일)
5. Meta 개발자 화면·권한 이름·Graph API 용어는 **구매자 채팅에 쓰지 말 것** — 이 문서/`설명서_인스타연결.md`에만

## 구매자 화면 원칙

- 설치·`.env`·프로필 폴더·dry-run 같은 말 금지 → **테스트발행 / 즉시발행 / 예약발행**
- 연결 UI = “연결 키 1(긴 글) / 연결 키 2(숫자)” 만
- 콘셉트·업종·타겟 칩 없음 → **주제 슬롯(최대 5)** 만
- 설정에서 본문 템플릿(직접 / AI 초안) · 하루 발행 남은 횟수 표시

## 발행 한도 (관리자)

- 구매자 기본 **하루 3회** (서울 날짜)
- 관리자 화면에서 구매자별 **하루 발행 한도** 수정 · **오늘 사용량** 확인
- 한도 초과 시 구매자에게 「추가 발행은 관리자에게 문의하세요」
- **테스트발행**은 한도 차감 없음 / 즉시·예약의 **실제 성공 게시**만 차감

## 예약 발행 한계

Streamlit Cloud에는 상시 워커가 없습니다.  
예약은 `data/schedules.json`에 저장되고, **앱을 열 때** 기한이 된 작업만 처리합니다.  
구매자에게 “예약 시간에 앱(또는 링크)을 한 번 열어 주세요”라고 안내하세요.

## (선택) zip 납품

오프라인·자체 PC 실행이 필요할 때만. 체크리스트:

- [ ] 코드 + fonts + requirements + run.bat/run.sh + `.env.example`
- [ ] 세 설명서 포함 (실행 방법은 웹 우선)
- [ ] `.env` / `.venv` / `.git` / 캐시 / 비밀값 제외
- [ ] `data/buyers.json` · `buyer_settings.json` · `schedules.json` 제외
- [ ] Main 실행은 `app_simple.py`

고객별 `clients/<이름>/` 폴더는 **고급 UI(`app.py`)** 또는 판매자 내부용입니다. 구매자 기본 경로에서는 숨깁니다.

## 절대 하지 말 것

- 비공식 인스타 봇 / 셀레니움 로그인 자동화
- 구매자 연결 키를 판매자 PC·공개 채팅·깃에 남기기
- 구매자에게 키를 공개 채팅에 붙여 넣으라고 안내하기
- zip에 `.env`, 실고객 산출물·토큰 포함

## 면책 (구매자 안내 문구 예시)

> 본 서비스는 공식 인스타그램 연결 방식으로만 게시합니다.  
> 연결 키·계정·게시 내용에 대한 책임은 사용자에게 있습니다.  
> 연결 키는 다른 사람에게 보내지 마세요.

## Streamlit Secrets (배포 시 선택)

Cloud Secrets에 토큰을 넣으면 데모 계정으로 바로 올릴 수 있습니다.  
구매자가 화면에 키를 붙여 넣으면 **화면 값이 우선**합니다. 예시는 `.streamlit/secrets.toml.example`.


## 구매자 계정 / 관리자

1. Streamlit Cloud Secrets (또는 로컬 `.streamlit/secrets.toml`)에 설정:
   - `ADMIN_USERNAME` = 관리자 아이디
   - `ADMIN_PASSWORD` = 관리자 비밀번호 (깃·README·채팅에 올리지 마세요)
   - (선택) `ADMIN_PASSWORD_HASH` = `sha256$salt$digest` — 있으면 해시로 검증
   - (선택) `ADMIN_GATE` / 이메일 OTP — 기본 사용 안 함
2. 관리자 열기: 앱 URL → **관리자 로그인** 탭(또는 사이드바 **admin**) → 아이디/비밀번호 로그인

```text
https://YOURAPP.streamlit.app/  → 「관리자 로그인」탭 또는 사이드바 admin
```

3. 구매자 아이디·비밀번호·메모 생성 · 필요 시 **하루 발행 한도** 조정
4. **로컬:** `data/buyers.json` 자동 저장 (깃 커밋 금지)
5. **Cloud:** 관리자 화면의 Secrets 블록을 Cloud Secrets에 붙여 넣기
6. 구매자에게는 **메인 URL** + 구매자 아이디/비밀번호만 전달 (관리자 계정·Secrets 금지)
