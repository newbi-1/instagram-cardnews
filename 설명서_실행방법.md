# 실행 방법 (Windows 기준)

인스타그램 카드뉴스 생성 앱을 PC에서 실행하는 방법입니다.

## 1. Python 설치

1. [Python 공식 사이트](https://www.python.org/downloads/)에서 **Python 3.10 이상**을 다운로드합니다.
2. 설치 시 **"Add python.exe to PATH"** 를 반드시 체크합니다.
3. 설치가 끝나면 새 명령 프롬프트(cmd)를 열고 아래를 입력해 확인합니다.

```bat
python --version
```

버전이 보이면 성공입니다. (`python`이 안 되면 `py --version`도 시도해 보세요.)

## 2. 압축 풀기

1. 받은 zip 파일을 원하는 폴더(예: `C:\인스타카드뉴스`)에 풉니다.
2. 폴더 안에 `run.bat`, `app.py`, `requirements.txt`, `.env.example` 이 있는지 확인합니다.

## 3. 환경 파일 준비

1. `.env.example` 파일을 복사해서 이름을 `.env` 로 바꿉니다.
2. 인스타 발행까지 쓰려면 `.env`에 토큰을 넣습니다. → 자세한 내용은 `설명서_인스타연결.md` 참고.
3. **미리보기·PNG 생성만** 할 때는 토큰 없이도 바로 실행할 수 있습니다.

## 4. run.bat 실행

1. 압축 푼 폴더에서 **`run.bat`을 더블클릭**합니다.
2. 처음 실행 시 가상환경(`.venv`) 생성과 패키지 설치가 자동으로 진행될 수 있습니다. 인터넷이 필요합니다.
3. 설치가 끝나면 브라우저가 열리거나, 터미널에 `http://localhost:8501` 같은 주소가 표시됩니다.
4. 브라우저가 안 열리면 Chrome/Edge 주소창에 `http://localhost:8501` 을 직접 입력하세요.

macOS / Linux는 `./run.sh` 를 사용합니다.

## 5. 화면(탭) 사용법 — 4단계

앱 상단(또는 사이드)에 대략 다음 흐름이 있습니다.

| 순서 | 탭/화면 | 할 일 |
|------|---------|--------|
| 1 | **프로필** | 고객(매장) 프로필을 고르거나 새로 만듭니다. 업종/콘셉트(카페, 학원 등)를 먼저 선택합니다. |
| 2 | **새 카드뉴스** | 주제·타겟·스타일을 고르고, 본문 소스 텍스트를 붙여 넣은 뒤 **생성**합니다. |
| 3 | **미리보기** | 만들어진 1080×1080 PNG 슬라이드(5~8장)를 확인합니다. 마음에 들면 저장 경로(`clients/이름/outputs/`)를 기억해 둡니다. |
| 4 | **발행 전 확인** | 기본은 **dry-run(연습)** 입니다. 실제 인스타 발행은 `.env` 토큰이 있을 때만, 확인 후 진행하세요. |

### 팁

- 소스 텍스트는 번호 목록(1. 2. 3.)이나 짧은 단락으로 쓰면 슬라이드가 잘 나뉩니다.
- 사진 배경은 무료 이미지 캐시를 사용합니다. 첫 생성 시 인터넷이 필요합니다.
- 문제가 생기면 터미널 창의 빨간 에러 메시지를 캡처해 판매자에게 문의하세요.

## 수동 실행 (run.bat이 안 될 때)

```bat
cd 압축푼폴더경로
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
streamlit run app.py
```

## 폴더 구조 (참고)

```
clients/<프로필명>/profile.json
clients/<프로필명>/outputs/*.png
assets/fonts/     ← Pretendard 폰트
assets/cache/     ← 사진 캐시 (자동 생성)
.env              ← 본인 토큰 (절대 공유 금지)
```
