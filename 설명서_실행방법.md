# 실행 방법 (구매자용)

이 앱은 **웹 브라우저만** 있으면 됩니다. Python 설치·압축 풀기·`.env` 파일은 **필요 없습니다.**

---

## 웹으로 쓰기 (권장 · 구매자)

판매자(또는 안내 메시지)가 보내 준 **링크**를 엽니다.  
자세한 안내는 앱 왼쪽 **설정·도움말**에 모두 들어 있습니다. (이 md는 백업용)

1. 판매자에게 받은 **아이디·비밀번호**로 로그인
2. 왼쪽 **설정**에서 **주제**(최대 5, 켜기/끄기) · **본문 템플릿** · (선택) **연결 키1·키2** 저장  
   (저장 후 마스킹만 표시 · 공개 채팅에 키 붙여넣기 금지)
3. 본문에서 **켜 둔 주제**를 고르고 **카드 만들기** → 미리보기  
   (마지막 장 = 참여유도 · 뉴스는 무료 구글 뉴스 RSS)
4. (선택) **테스트발행** / **즉시발행** / **예약발행**  
   - 테스트 = 실제로 안 올림 · 하루 한도 미차감  
   - 즉시 = 연결 필요 · 성공 시 한도 차감  
   - 예약 = 시간 저장 · 앱을 열 때 처리 (한도는 실제 발행 시)

팁:

- 주제는 짧게 (예: 신메뉴, 봄 할인).
- 본문 템플릿의 `{topic}` / `{주제}`는 선택한 주제로 바뀝니다.
- 설치·폴더·`.env`는 필요 없습니다. 배경은 **무료 이미지 소스**(비용 없음).
- 하루 발행 한도(기본 3회, 서울 날짜)를 넘으면 판매자에게 문의하세요.

문제가 나면 화면 메시지를 캡처해 판매자에게 보내 주세요. (키 전체는 캡처에 넣지 마세요)

---

## (고급 · 선택) PC에서 zip으로 실행

판매자·개발자·오프라인용입니다. 일반 구매자는 **웹 링크**만 쓰면 됩니다.

1. Python 3.10+ 설치 (PATH 체크)
2. zip 압축 해제 → `run.bat`(Windows) 또는 `./run.sh`(Mac/Linux) 더블클릭
3. 브라우저에서 `http://localhost:8501` 이 열리면 웹과 같은 **간단 화면**이 나옵니다
4. 인스타 연결도 화면에서 키 붙여넣기 (`.env` 없이도 가능)

수동 실행:

```bat
cd 압축푼폴더
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
streamlit run app_simple.py
```

프로필 폴더를 여러 개 쓰는 **고급 UI**는 `streamlit run app.py` 입니다.
