# 인스타그램 연결 방법 (공식 Meta API)

이 앱은 **비공식 봇을 쓰지 않습니다.** Meta(페이스북)에서 발급한 **공식 Instagram Graph / Instagram Login 토큰**만 사용합니다.

토큰·계정 설정은 **구매자(매장) 본인 책임**입니다. 판매자가 대신 로그인하거나 토큰을 보관하지 않는 것을 권장합니다.

## 준비물

- 인스타그램 **비즈니스** 또는 **크리에이터** 계정
- Meta 개발자 계정 (facebook.com → 개발자)
- PC에서 앱을 실행할 수 있는 환경 (`.env` 편집)

## 한눈에 보는 순서

1. Meta 개발자에서 앱 만들기  
2. Use cases에서 **Instagram API** 관련 항목 추가  
3. 테스터(Tester)로 본인 계정 추가 후 수락  
4. Instagram 계정 연결 / 토큰 발급  
5. `IG_USER_ID` 확인  
6. 프로젝트 `.env`에 값 넣기  
7. 앱에서 dry-run 끄고 발행 테스트  

---

## 1. Meta 앱 만들기

1. [Meta for Developers](https://developers.facebook.com/)에 로그인합니다.
2. **내 앱** → **앱 만들기**를 진행합니다.
3. 용도/유형은 안내에 따라 선택합니다. (비즈니스용 카드뉴스 발행 목적)

## 2. Use cases — Instagram API

1. 앱 대시보드에서 **Use cases**(사용 사례)를 엽니다.
2. **Instagram API** / Instagram 관련 사용 사례를 추가·활성화합니다.
3. 권한이 부족하면 앱이 테스트 모드일 때 **테스터 계정**으로만 동작합니다. (아래 3단계)

> 화면 문구는 Meta가 자주 바꿉니다. "Instagram" / "Instagram API" / "Instagram Login" 이 보이면 그 경로를 따르면 됩니다.

## 3. 테스터 추가

1. 앱 역할(Roles) 또는 테스터 메뉴에서 **Instagram 테스터**로 본인 IG 계정을 추가합니다.
2. 인스타그램 앱/웹에서 **테스터 초대 수락**을 꼭 합니다. (수락 전이면 토큰이 안 되거나 발행이 실패합니다.)

## 4. 토큰 발급 (Instagram Login)

실무에서 잘 되는 경로 요약:

1. Instagram Login / API 설정에서 계정을 연결합니다.
2. **Access Token**을 발급·복사합니다.
3. Instagram Login으로 받은 토큰은 보통 **`IG`로 시작**합니다. (예: IG로 시작하는 긴 문자열)
4. Facebook Page/시스템 사용자 토큰은 보통 **`EAA`로 시작**합니다.

이 앱은 토큰 접두어로 Graph 호스트를 자동 선택합니다.

| 토큰 형태 | Graph 호스트 |
|-----------|--------------|
| `IG…` 로 시작 | `https://graph.instagram.com/v21.0` |
| `EAA…` 등 | `https://graph.facebook.com/v21.0` |

단기 토큰은 만료됩니다. 장기 토큰(또는 재발급) 방법은 Meta 문서를 따릅니다.

## 5. IG_USER_ID 찾기

1. Meta 그래프 탐색기 또는 Instagram 계정 정보 API로 **Instagram 사용자 ID(숫자)** 를 확인합니다.
2. 프로필 ID / 비즈니스 계정 ID와 혼동하지 마세요. 앱이 요구하는 값은 **`IG_USER_ID`** 입니다.
3. 연결이 정상이면 숫자 ID가 나옵니다. `.env`의 `IG_USER_ID=` 뒤에 그대로 넣습니다.

## 6. `.env`에 넣기

프로젝트 루트의 `.env.example`을 복사해 `.env`를 만든 뒤 아래처럼 채웁니다.

```env
IG_ACCESS_TOKEN=여기에_발급받은_토큰
IG_USER_ID=여기에_숫자_ID
DEFAULT_PROFILE=demo
```

주의:

- `.env`는 **절대 zip·카톡·깃허브에 올리지 마세요.**
- 토큰이 유출되면 Meta에서 즉시 폐기·재발급하세요.
- 값이 바뀌면 앱(Streamlit)을 재시작하면 반영됩니다.

## 7. 이미지 URL (무료 업로드 경로)

Instagram Graph API는 캐러셀 이미지에 **공개 HTTPS URL**이 필요합니다.

이 앱의 `publisher`는 이미 다음 순서로 처리합니다.

1. 슬라이드 옆 `.url` 사이드카 파일이 있으면 그 URL 사용  
2. `.env`의 `IG_IMAGE_BASE_URL`이 있으면 `베이스/파일명` 사용  
3. 없으면 **임시 공개 호스트**로 로컬 PNG→JPEG 업로드 후 URL 사용 (litterbox / 0x0 / uguu 등)

즉, 별도 CDN이 없어도 **무료 임시 업로드 경로**로 발행 테스트가 가능합니다.  
운영에서는 자체 CDN/`IG_IMAGE_BASE_URL`을 권장합니다.

## 8. 앱에서 발행

1. 카드뉴스를 생성·미리보기합니다.
2. **발행 전 확인** 화면에서 dry-run이 켜져 있으면 실제 업로드는 하지 않습니다.
3. 토큰이 준비되면 dry-run을 끄고 발행을 진행합니다.
4. 실패 시 메시지(권한, 토큰 만료, 테스터 미수락, 미디어 컨테이너 오류 등)를 확인하세요.

## 자주 하는 실수

| 증상 | 점검 |
|------|------|
| 자격 증명 없음 | `.env`에 `IG_ACCESS_TOKEN` / `IG_USER_ID` 둘 다 있는지 |
| 권한 오류 | 테스터 수락, 앱 Use case, 계정 유형(비즈니스/크리에이터) |
| 토큰 만료 | 재발급 후 `.env` 갱신, 앱 재시작 |
| 이미지 URL 오류 | 인터넷 허용, 임시 업로드 차단 여부, 또는 `IG_IMAGE_BASE_URL` 설정 |
| `IG…` 토큰인데 Facebook Graph만 호출 | 최신 앱인지 확인 (자동 분기 포함 버전) |

## 면책

- Meta 정책·요금·심사·토큰 정책은 Meta 측 변경에 따릅니다.
- 비공식 자동화/매크로/계정 공유는 지원하지 않으며, 계정 제재 위험이 있습니다.
- 토큰 보관·유출·만료 관리는 구매자 책임입니다.
