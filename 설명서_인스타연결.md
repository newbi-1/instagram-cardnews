# 인스타그램 연결 (판매자·기술용)

**구매자 화면에는 이 문서의 용어를 쓰지 마세요.**  
구매자에게는 “연결 키 1(긴 글) / 연결 키 2(숫자)”만 안내합니다.

이 앱은 비공식 봇을 쓰지 않습니다. Meta 공식 Instagram Graph / Instagram Login 토큰만 사용합니다.

## 구매자에게 전달할 말 (복붙용)

> 인스타에 올리려면 화면에서 **연결하기**를 누르고,  
> 제가 드린 **연결 키 1**과 **연결 키 2**만 붙여 넣으세요.  
> 설치나 설정 파일은 필요 없습니다. 먼저 **연습**으로 눌러 보시고, 괜찮으면 **실제로 올리기**를 선택하세요.

- 연결 키 1 = `IG_ACCESS_TOKEN` (긴 문자열)
- 연결 키 2 = `IG_USER_ID` (숫자)

---

## 판매자가 키를 만드는 순서

1. Meta 개발자에서 앱 만들기  
2. Use cases에서 Instagram API 관련 항목 추가  
3. 테스터(Tester)로 계정 추가 후 수락  
4. Instagram Login / 토큰 발급  
5. `IG_USER_ID` 확인  
6. Streamlit Cloud Secrets **또는** 구매자 화면 붙여넣기로 전달  
7. 앱에서 **연습** 확인 후 **실제로 올리기**

### Meta 앱 · 토큰 요약

1. [Meta for Developers](https://developers.facebook.com/) → 앱 만들기  
2. Instagram API / Instagram Login 경로로 토큰 발급  
3. `IG…` 로 시작하면 `graph.instagram.com`, `EAA…` 등은 `graph.facebook.com` (앱이 자동 분기)  
4. Instagram 사용자 ID(숫자) = 연결 키 2  

로컬 고급용 `.env` 예:

```env
IG_ACCESS_TOKEN=발급_토큰
IG_USER_ID=숫자_ID
```

`.env` / Secrets / 실토큰은 **절대 zip·카톡·깃에 올리지 마세요.**

### 이미지 URL

Graph API는 공개 HTTPS 이미지 URL이 필요합니다.  
`publisher`가 `.url` 사이드카 → `IG_IMAGE_BASE_URL` → 임시 공개 업로드 순으로 처리합니다.  
별도 CDN 없이도 발행 테스트 가능합니다.

### 자주 하는 실수

| 증상 | 점검 |
|------|------|
| 연결 안 됨 | 키 1·2 모두 있는지, 공백·따옴표 없는지 |
| 권한 오류 | 테스터 수락, 비즈니스/크리에이터 계정 |
| 토큰 만료 | 재발급 후 Secrets 또는 화면 키 갱신 |
| 이미지 URL 오류 | 서버 인터넷, 임시 업로드 차단, 또는 `IG_IMAGE_BASE_URL` |

## 면책

Meta 정책·토큰·계정 관리는 키를 보유한 쪽 책임입니다. 비공식 자동화는 지원하지 않습니다.
