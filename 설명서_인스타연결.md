# 인스타그램 연결 안내

이 앱은 비공식 봇을 쓰지 않습니다. Meta 공식 연결(토큰)만 사용합니다.

## 구매자용 (화면 그대로)

**실제 발행에는 본인 인스타 연결 키가 필수**예요.  
판매자 공용 키에만 의존하지 마세요. (판매자 Secrets의 IG는 데모·테스트용일 수 있음)

### 초간단 순서

1. 인스타를 **비즈니스** 또는 **크리에이터** 계정으로 준비해요.
2. Meta(페이스북) 개발자에서 앱을 만들고 토큰을 발급받아요.
3. 앱 왼쪽 **설정**에서
   - **연결 키1** = 긴 글(액세스 토큰)
   - **연결 키2** = 숫자 계정 번호
4. 각 칸 아래 **사용법** 버튼을 누르면 그 칸만의 짧은 안내가 열려요.
5. **저장·연결** → 화면에는 마스킹만 보여요.

미리보기·**테스트발행**은 연결 없이 가능합니다.  
**즉시발행·예약발행**은 본인 키 연결이 필수입니다.

키를 **카톡·공개 채팅·깃허브**에 붙여 넣지 마세요.

### 구매자에게 전달할 말 (복붙용)

> 인스타에 실제로 올리려면 왼쪽 **설정**에서 **본인** 연결 키 1·2를 넣어 주세요.  
> 각 칸 아래 **사용법**을 누르면 단계가 나와요.  
> 먼저 **테스트발행**으로 연습한 뒤 **즉시발행**을 쓰세요.

---

## 판매자·기술용 (구매자 채팅에 이 용어 쓰지 말 것)

| 화면 말 | 기술 값 |
|---|---|
| 연결 키1 | `IG_ACCESS_TOKEN` (긴 문자열, `IG…` 또는 `EAA…`) |
| 연결 키2 | `IG_USER_ID` (숫자) |

### 키 발급 요약

1. [Meta for Developers](https://developers.facebook.com/) → 앱 만들기
2. Instagram API / Instagram Login 경로로 토큰 발급
3. 테스터 계정 추가·수락 (개발 모드일 때)
4. `IG…` → `graph.instagram.com` / `EAA…` → `graph.facebook.com` (앱이 자동 분기)
5. 구매자는 **설정 UI**에 본인 키를 붙여 넣음 (권장)
6. (선택) 판매자 데모용으로만 Streamlit Secrets `IG_*` — 구매자 발행 경로의 필수 조건은 아님

로컬 고급 `.env` 예 (판매자 PC 전용):

```env
IG_ACCESS_TOKEN=발급_토큰
IG_USER_ID=숫자_ID
```

`.env` / Secrets / 실토큰은 **절대 zip·카톡·공개 채팅·깃에 올리지 마세요.**  
구매자 키는 `data/buyer_settings.json`(gitignore) 또는 세션에만 두며, UI에서는 마스킹만 표시합니다.

### 이미지 URL

Graph API는 공개 HTTPS 이미지 URL이 필요합니다.  
`publisher`가 임시 공개 업로드 등으로 처리합니다.

### 자주 하는 실수

| 증상 | 점검 |
|------|------|
| 연결 안 됨 | 키1·키2 모두 있는지, 같은 계정인지 |
| 권한 오류 | 비즈니스/크리에이터, 테스터 수락 |
| 토큰 만료 | 재발급 후 설정에서 다시 저장 |
| 발행만 안 됨 | 본인 키로 저장했는지 (공용 Secrets만으로는 부족할 수 있음) |

## AI 본문 키 (BYOK)

「AI에게 맡기기」는 구매자가 설정에서 고른 제공자(Gemini / OpenAI / Claude / Grok / OpenRouter)의 **본인 API 키**로만 호출합니다.  
각 입력 칸 아래 **사용법** 참고. 키 없으면 「키 없이 간단 초안」만 사용합니다. 요금은 구매자 부담입니다.

## 면책

Meta·AI 제공자 정책·토큰·계정·비용은 키를 보유한 쪽 책임입니다. 비공식 자동화는 지원하지 않습니다.
